services:
  - type: web
    name: tts-flask
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt && python download_model.py
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
